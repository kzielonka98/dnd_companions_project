@using Microsoft.AspNetCore.Identity;
@inject SignInManager<UserModel> SignInManager
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a class="nav-link text-white" asp-controller="Characters" asp-action="Index">Characters</a>
    </li>
}
    